# Configuración de Netlify
# Los redirects están en el archivo _redirects

[build]
  publish = "."

# Nota: Para activar el sitio completo cuando esté listo:
# 1. Elimina el archivo _redirects
# 2. Haz commit y push a GitHub
